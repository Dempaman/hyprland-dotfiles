# Hyprlock config using Nord colors (blue, white, gray only)

# Nord theme references
$color11 = rgba(4C566Aff)  # gray
$color12 = rgba(81A1C1ff)  # blue
$color13 = rgba(D8DEE9ff)  # light foreground

$Scripts = $HOME/.config/hypr/scripts

source = ~/.config/hypr/wallust/hyprland-colors-nord

general {
    grace = 1
    fractional_scaling = 2
    immediate_render = true
}

background {
    monitor =
    path = $HOME/.config/hypr/wallpaper_effects/.wallpaper_current
    color = rgb(30, 36, 48)  # Nord base fallback
    blur_size = 3
    blur_passes = 2
    noise = 0.0117
    contrast = 1.3000
    brightness = 0.8000
    vibrancy = 0.2100
    vibrancy_darkness = 0.0
}

# Date
label {
    monitor =
    text = cmd[update:18000000] echo "<b> $(date +'%A, %-d %B') </b>"
    color = $color13
    font_size = 16
    font_family = Iosevka Nerd Font Medium 
    position = 0, -120
    halign = center
    valign = center
}

# Hour-Time
label {
    monitor =
    text = cmd[update:1000] echo "$(date +"%H")"
    color = $color13
    font_size = 200
    font_family = JetBrainsMono Nerd Font ExtraBold
    position = 0, -60
    halign = center
    valign = top
}

# Minute-Time
label {
    monitor =
    text = cmd[update:1000] echo "$(date +"%M")"
    color = $color12
    font_size = 200
    font_family = JetBrainsMono Nerd Font ExtraBold
    position = 0, -320
    halign = center
    valign = top
}

# Seconds-Time
label {
    monitor =
    text = cmd[update:1000] echo "$(date +"%S")"
    color = $color11
    font_size = 35
    font_family = JetBrainsMono Nerd Font ExtraBold
    position = 0, -350
    halign = center
    valign = top
}

# # USER
# label {
#     monitor =
#     text =   $USER
#     color = $color13
#     font_size = 24
#     font_family = Iosevka Nerd Font Medium 
#     position = 0, 150
#     halign = center
#     valign = bottom
# }

# INPUT FIELD
input-field {
    monitor =
    size = 300, 50
    outline_thickness = 1
    dots_size = 0.2
    dots_spacing = 0.2
    dots_center = true
    outer_color = rgba(255, 255, 255, 0.03)
    inner_color = rgba(255, 255, 255, 0.14)
    capslock_color = rgba(255, 255, 255, 0.45)
    font_color = $color13
    fade_on_empty = false
    font_family = Iosevka Nerd Font Medium 
    placeholder_text = <span foreground ="##e5e9f047" >Type Password</span>
    hide_input = false
    position = 0, 50
    halign = center
    valign = bottom
}

# Uptime
label {
    monitor =
    text = cmd[update:60000] echo "<b> $(uptime -p || $Scripts/UptimeNixOS.sh) </b>"
    color = $color13
    font_size = 16
    font_family = Iosevka Nerd Font Medium 
    position = 0, 0
    halign = right
    valign = bottom
}

# Weather
label {
    monitor =
    text = cmd[update:3600000] [ -f "$HOME/.cache/.weather_cache" ] && cat "$HOME/.cache/.weather_cache"
    color = $color13
    font_size = 16
    font_family = Iosevka Nerd Font Medium 
    position = 50, 0
    halign = left
    valign = bottom
}
